apiVersion: v1
kind: Service
metadata:
  name: {{ include "fincontrol-front.fullname" . }}
  labels:
    {{- include "fincontrol-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fincontrol-front.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
